
Run make to compile the unit test.

Run ./edflib_test to run the test. If there are no errors, it doesn't output anything.

In case there's an error, it exit with an error message telling in which line the problem is.

Run make clean to cleanup the directory and remove all files generated by the test.
